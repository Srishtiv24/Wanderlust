<% layout('/layouts/boiler-plate') %>

<div class="wl-auth-page">
  <div class="wl-auth-card">
    <div class="wl-auth-logo">
      <div class="brand-circle">
        <i class="fa-regular fa-compass"></i>
      </div>
      <h2>Wanderlust</h2>
      <p>Welcome back, Traveler</p>
    </div>

    <form method="post" action="/login" class="needs-validation" novalidate>
      <div class="wl-form-group">
        <label class="wl-form-label">Username</label>
        <input type="text" name="username" class="wl-form-control" placeholder="Enter your username" required>
        <div class="invalid-feedback" style="font-size:0.75rem;">Please enter your username.</div>
      </div>

      <div class="wl-form-group">
        <label class="wl-form-label">Password</label>
        <input type="password" name="password" class="wl-form-control" placeholder="Enter your password" required>
        <div class="invalid-feedback" style="font-size:0.75rem;">Please enter your password.</div>
      </div>

      <button type="submit" class="wl-submit-btn" style="width:100%; padding: 0.75rem; font-size: 0.95rem;">
        <i class="fa-solid fa-right-to-bracket"></i> Log In
      </button>
    </form>

    <p style="text-align:center; font-size:0.82rem; color: var(--text-muted); margin-top: 1.25rem;">
      New to Wanderlust? <a href="/signup" style="color: var(--primary); font-weight: 600; text-decoration: none;">Create an account</a>
    </p>
  </div>
</div>

<script>
(function() {
  'use strict';
  const forms = document.querySelectorAll('.needs-validation');
  Array.from(forms).forEach(form => {
    form.addEventListener('submit', event => {
      if (!form.checkValidity()) {
        event.preventDefault();
        event.stopPropagation();
      }
      form.classList.add('was-validated');
    }, false);
  });
})();
</script>
