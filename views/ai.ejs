<% layout('/layouts/boiler-plate') %>

<div class="ai-page">

  <!-- SIDEBAR -->
  <aside class="ai-sidebar">
    <div class="ai-sidebar-header">
        <div class="ai-sidebar-logo ai-orb-avatar">✦</div>
      <div>
        <div class="ai-sidebar-name">Travel Assistant</div>
      </div>
    </div>

    <button class="ai-new-btn" onclick="newChat()">
      <i class="fa-regular fa-pen-to-square"></i> New conversation
    </button>

    <nav class="ai-preset-nav">

      <div class="ai-category">
        <div class="ai-category-label">Browse by Type</div>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Show me all beach listings</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Show me mountain and ski stays</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Show me unique stays like treehouses or castles</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Show me city apartment listings</span>
        </button>
      </div>

      <div class="ai-category">
        <div class="ai-category-label">Find by Budget</div>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>What listings are under ₹1000/night?</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Best value listings on Wanderlust</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>What are the most luxurious listings?</span>
        </button>
      </div>

      <div class="ai-category">
        <div class="ai-category-label">By Destination</div>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Do you have listings in Asia?</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Show listings in Europe</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Any listings in the USA?</span>
        </button>
      </div>

      <div class="ai-category">
        <div class="ai-category-label">Help Me Choose</div>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Best listing for a romantic trip</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>Best listing for a solo traveller</span>
        </button>
        <button onclick="sendPreset(this)" class="ai-preset-item">
          <span>What is the most popular listing?</span>
        </button>
      </div>

    </nav>
  </aside>

  <!-- MAIN CHAT -->
  <div class="ai-main">

    <!-- Header -->
    <header class="ai-header">
      <div class="ai-header-left">
        <span class="ai-header-title">AI Travel Assistant</span>
        <span class="ai-header-sep">·</span>
        <span class="ai-header-status" id="aiStatusLine">
          <span class="ai-dot" id="aiDot"></span>
          <span id="aiStatusText">Ready</span>
        </span>
      </div>
      <button class="ai-header-action" onclick="newChat()" title="New chat">
        <i class="fa-regular fa-pen-to-square"></i>
        <span>New chat</span>
      </button>
    </header>

    <!-- Messages -->
    <div class="ai-messages" id="aiMessages">

      <div class="ai-welcome" id="aiWelcome">
        <div class="ai-welcome-inner"> 

            <div class="ai-welcome-star">✦</div>
          <h2>How can I help you today<% if(currUser){ %>, <%= currUser.username.charAt(0).toUpperCase() + currUser.username.slice(1) %><% } %>?</h2>
          <p>Ask me to find the perfect Wanderlust listing, compare stays, or help you choose based on your budget and destination.</p>

          <div class="ai-suggestions">
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-umbrella-beach"></i>
              <span>Show me all beach listings</span>
            </button>
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-mountain-sun"></i>
              <span>Show me mountain and ski stays</span>
            </button>
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-indian-rupee-sign"></i>
              <span>What listings are under ₹1000/night?</span>
            </button>
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-crown"></i>
              <span>What are the most luxurious listings?</span>
            </button>
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-heart"></i>
              <span>Best listing for a romantic trip</span>
            </button>
            <button class="ai-suggestion" onclick="sendPreset(this)">
              <i class="fa-solid fa-earth-americas"></i>
              <span>Show listings in Europe</span>
            </button>
          </div>
        </div>
      </div>

    </div>

    <!-- Input -->
    <div class="ai-input-area">
      <div class="ai-input-box" id="aiInputBox">
        <textarea
          id="aiInput"
          placeholder="Ask about listings, prices, destinations…"
          rows="1"
          onkeydown="handleKey(event)"
          oninput="autoResize(this)"
        ></textarea>
        <button class="ai-send" id="aiSendBtn" onclick="sendMessage()">
          <i class="fa-solid fa-arrow-up"></i>
        </button>
      </div>
    </div>

  </div>
</div>

<% if (typeof autoPrompt !== 'undefined' && autoPrompt) { %>
<script>
// Auto-send the prompt passed via ?prompt= query string (e.g. from itinerary or wishlist page)
window.addEventListener('DOMContentLoaded', function() {
  const prompt = `<%- autoPrompt.replace(/`/g, "'") %>`;
  if (prompt && typeof sendMessage === 'function') {
    const input = document.getElementById('aiInput');
    if (input) {
      input.value = prompt;
      setTimeout(() => sendMessage(), 250);
    }
  }
});
</script>
<% } %>
